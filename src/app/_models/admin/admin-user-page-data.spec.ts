import { AdminUserPageData } from './admin-user-page-data';

describe('AdminUserPageData', () => {
  it('should create an instance', () => {
    expect(new AdminUserPageData()).toBeTruthy();
  });
});
